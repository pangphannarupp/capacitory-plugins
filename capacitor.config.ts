import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'phanna.capacitor.plugin.sample',
  appName: 'phanna.capacitor.plugin.sample',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
